# Backend Configuration
# Simplified run (no Docker): use SQLite and leave USE_CELERY=false
DATABASE_URL=sqlite:///./agentic_ai.db
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agentic_ai
REDIS_URL=redis://localhost:6379
USE_CELERY=false
SECRET_KEY=your-secret-key-here-change-in-production

# --- Local development (no API keys required) ---
# Leave AI keys empty and set USE_LOCAL_LLM=true. Install Ollama (https://ollama.ai) and run e.g. `ollama run qwen3:8b`.
# AI API Keys (optional; used when USE_LOCAL_LLM=false or when local LLM is unavailable)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Email Service (SendGrid, AWS SES, etc.)
EMAIL_SERVICE_API_KEY=your-email-service-key
IMAP_HOST=imap.gmail.com
IMAP_PORT=993

# Calendar Integration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Stripe Payment (optional; leave empty for local-only)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Application Settings
ENVIRONMENT=development
DEBUG=true
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Local LLM (default: true so app runs without API keys)
USE_LOCAL_LLM=true
LOCAL_LLM_BACKEND=ollama
LOCAL_MODEL_NAME=qwen3:8b
OLLAMA_HOST=http://localhost:11434

# Build: Synthesis-style = one index.html to open in browser (inline CSS/JS, no server needed)
BUILD_SINGLE_FILE=true